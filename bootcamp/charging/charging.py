def gp(strt_value: int) -> "list[float]" :
    prev = 100
    list_of_charges =[]
    while gp(prev) != 0:
        prev = prev * 2 / 3
        list_of_charges.append(prev)
    return list_of_charges
def time_elapsed(charges: "list[float]") -> float:
    time_gone 
    for i in charges:

